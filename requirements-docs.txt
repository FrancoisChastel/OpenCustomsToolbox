# Documentation toolchain for the Sydonia Toolkit docs site.
# Install:  python -m venv .venv-docs && . .venv-docs/bin/activate && pip install -r requirements-docs.txt
# Serve:    mkdocs serve
# Build:    mkdocs build --strict
mkdocs-material>=9.5,<10
pymdown-extensions>=10.7
mike>=2,<3  # doc versioning (versions deployed to gh-pages)
